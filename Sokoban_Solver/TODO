Heuristic improvement:
For each box, compute a wavefront map, for fast lookup of cost from one position to another position.
Try to implement minimum cost matching, may be difficult/recquire further changes.

